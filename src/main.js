import "./style.css";

const total = () => {
  let a = prompt("a: "); // base
  let b = prompt("b: "); // 90
  let c = prompt("c: "); // big

  if (!a) {
    alert("You have to give atleast 2 numbers");
  }
};

total();
